pipeline{
    agent any

    stages{
        steps{
            stage('Build'){
                sh '''
                        echo "git connection is successful"
                '''
            }
        }
    }
}